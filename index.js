const names = ["yanfer", "daniel", "araque"];
for (let i = 0; i < names.length; i++) {
 console.log(names[i]);
}

const showUserInfo = (userName, userAge) =>
 `The user name is ${userName}, the user age is ${userAge}`;

console.log(showUserInfo("Anthony", "31"));
